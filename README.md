Configuration files for the different versions of FlowOS, based around the particular users' needs. If using to compile the Linux kernel, users will need to rename the file to .config and place it inside the top folder of the kernel's folder. Depending on if the FlowOS kernel branches off from the root Linux kernel, some options may not work with the FlowOS kernel, and vice versa. 
